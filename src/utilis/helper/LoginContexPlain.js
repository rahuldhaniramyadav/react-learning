import { createContext } from "react";

// creating a context
const LoginContexPlain = createContext("RY Plain");

// const LoginContext = createContext("RY Default111");

export default LoginContexPlain;
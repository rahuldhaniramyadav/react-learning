const GroceryMart = () => {
  return (
    <h2>This is component is used for handling the different type of caterories to make the different bundling instead of using the default one</h2>
  )
}

export default GroceryMart;
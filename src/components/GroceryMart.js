const GroceryMart = () => {
  return (
    <h2>This is component is used for handling the different type of categories to make the different bundling instead of using the default by parcel</h2>
  )
}

export default GroceryMart;